================================================================================
ECHOMANCER V2 — COMPLETE CODEBASE FIX PROMPT
================================================================================

PROJECT: Echomancer v2 (Next.js 16 + Modal F5-TTS + Turso + R2)
DEPLOYED AT: https://echomancer-v2.vercel.app
MODAL: https://ntemusejoel--echomancer-f5-tts-fastapi-app.modal.run

YOUR MISSION:
Fix every bug in this codebase AND completely remove the YouTube feature.
Work systematically through the priority list below. Build and test after each 
major group of changes. Push to git when done.

================================================================================
PHASE 0: COMPLETELY REMOVE YOUTUBE FEATURE
================================================================================

Delete these files entirely:
- src/app/api/youtube/search/route.ts
- src/app/api/youtube/download/route.ts
- src/app/api/youtube/ (the entire directory)

Remove from src/app/api/jobs/route.ts:
- Remove `videoId` from the job creation payload sent to Modal
- Remove `videoId` from the validation schema import if still referenced
- The job creation POST should only send: job_id, pdf_r2_key, voice_r2_key, 
  start_time, end_time, webhook_url, book_title, voice_name, r2_bucket_name, 
  r2_account_id, r2_access_key_id, r2_secret_access_key

Remove YouTube from src/app/dashboard/voice/page.tsx:
- Delete the entire "youtube" tab and all its related state/handlers:
  - Remove `selectedVideo`, `startTime`, `endTime`, `isDownloading` state
  - Remove `handleSelectVideo`, `handleBackToSearch`, `handleDownloadClip`
  - Remove `formatTime` helper if only used for YouTube
  - Remove YouTube search results rendering
  - Remove YouTube player iframe
  - Remove time selection UI (slider, inputs, duration badge)
- Keep ONLY the "upload" tab and "saved" tab
- The upload tab flow: user uploads audio → redirects to /dashboard/voice/clip
  with voicePath in URL params
- Remove the `videoId` param from the router.push in handleUpload (it currently 
  pushes pdfPath, pdfName, voicePath, videoTitle, isUpload — keep those minus 
  any YouTube-specific ones)
- Clean up any unused imports (Search, Play, Scissors, CheckCircle2, etc. if 
  only used for YouTube)

Remove YouTube from src/app/dashboard/voice/clip/page.tsx:
- Remove `videoId` from URL param reads
- Remove any YouTube-specific logic
- The page should ONLY handle uploaded voice clips
- Keep: audio preview, slider for start/end time, create audiobook button

Remove from src/lib/env.ts:
- Remove `YOUTUBE_API_KEY` from the env schema

Remove from src/lib/validation.ts:
- Remove any YouTube-specific schemas if they exist

Update src/app/api/jobs/[id]/webhook/route.ts:
- Remove `video_id` from webhook payload handling if present

Update src/app/api/voices/route.ts:
- Remove any YouTube-specific source handling if it exists beyond the 
  "youtube" | "upload" enum (keep the enum value for existing data, just 
  remove UI for creating new YouTube-sourced voices)

================================================================================
PHASE 1: CRITICAL SECURITY FIXES
================================================================================

--- 1.1 COMMAND INJECTION (CRITICAL) ---
File: src/app/api/youtube/download/route.ts
Status: DELETE THIS FILE ENTIRELY (Phase 0)
If any other file uses ffmpeg with string interpolation, fix it by using 
execFile with array arguments instead of exec with string interpolation.

--- 1.2 PATH TRAVERSAL IN VOICE PREVIEW (CRITICAL) ---
File: src/app/api/voice/preview/route.ts
Line: ~62-63 (the downloadFile call)
Fix: Add path validation before downloading:
```typescript
const pathPattern = /^(voices|previews|audiobooks)\/[a-zA-Z0-9._/-]+$/;
if (!pathPattern.test(parsed.voiceStoragePath) || parsed.voiceStoragePath.includes("..")) {
  throw new AppError("INVALID_PATH", "Invalid voice storage path", 400);
}
```

--- 1.3 PATH TRAVERSAL IN STORAGE API (CRITICAL) ---
File: src/app/api/storage/[[...path]]/route.ts
Line: ~25-28
Fix: Add trailing path separator and normalize:
```typescript
const storageRoot = path.resolve(process.env.STORAGE_PATH || "./data/storage") + path.sep;
const resolvedPath = path.resolve(fullPath) + path.sep;
if (!resolvedPath.startsWith(storageRoot)) {
  return NextResponse.json({ error: "Invalid path" }, { status: 403 });
}
```
Also add: export const runtime = "nodejs"; (line 8 area)

--- 1.4 R2 SECRET KEY LEAK TO MODAL (CRITICAL) ---
File: src/app/api/jobs/route.ts
Line: ~60-88 (the fetch to Modal)
Fix: Do NOT send R2_SECRET_ACCESS_KEY to Modal. Instead:
Option A: Generate presigned URLs for the specific PDF and voice files, send 
those URLs to Modal instead of R2 credentials.
Option B: Store R2 credentials as Modal Secrets (environment variables on Modal 
side) and remove them from the request body entirely.
RECOMMENDED: Option B — remove r2_secret_access_key and r2_access_key_id from 
the request body. Update modal/f5_tts_server.py to read R2 credentials from 
environment variables (Modal Secrets). Update AudiobookRequest dataclass to 
remove credential fields.

--- 1.5 UNAUTHENTICATED WEBHOOK (CRITICAL) ---
File: src/app/api/jobs/[id]/webhook/route.ts
Line: ~16-56
Fix: Add secret validation:
```typescript
const WEBHOOK_SECRET = process.env.WEBHOOK_SECRET;
const authHeader = request.headers.get("x-webhook-secret");
if (WEBHOOK_SECRET && authHeader !== WEBHOOK_SECRET) {
  return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
}
```
Also add to .env.local template: WEBHOOK_SECRET=your-random-secret-here
Update modal/f5_tts_server.py send_webhook_sync to include the secret header.

--- 1.6 R2 METADATA DOWNLOADS ENTIRE FILE (CRITICAL) ---
File: src/lib/storage.ts
Line: ~223-243 (getFileMetadata)
Fix: For R2, use HeadObjectCommand instead of downloading:
```typescript
import { HeadObjectCommand } from "@aws-sdk/client-s3";
// In getFileMetadata for R2:
const response = await client.send(new HeadObjectCommand({ Bucket: R2_BUCKET_NAME, Key: key }));
return { size: response.ContentLength || 0, modified: response.LastModified || new Date() };
```

================================================================================
PHASE 2: MODAL SERVER FIXES
================================================================================

File: modal/f5_tts_server.py

--- 2.1 WEBHOOK STATUS CODE LOGIC (CRITICAL) ---
Line: ~217-231 (send_webhook_sync)
Change: `if response.status_code < 500:` → `if response.status_code < 400:`
4xx errors should NOT be treated as success.

--- 2.2 GPU MEMORY CLEANUP (CRITICAL) ---
Line: ~345-351 (model loading) and end of process_audiobook
Fix: Add after model inference is done, before cleanup():
```python
import torch
# At the end of process_audiobook, before finally/cleanup:
torch.cuda.empty_cache()
```
Also add `del model` before the empty_cache call.

--- 2.3 BASE64 DECODE OUTSIDE TRY/CATCH (CRITICAL) ---
Line: ~430 (_decode_audio_for_worker call)
Fix: Decode voice sample ONCE before the batch loop and reuse:
```python
# Before the batch loop:
try:
    ref_audio, ref_sr = _decode_audio_for_worker(voice_base64)
    max_samples = int(15 * ref_sr)
    if len(ref_audio) > max_samples:
        start = (len(ref_audio) - max_samples) // 2
        ref_audio = ref_audio[start:start + max_samples]
except Exception as e:
    raise ValueError(f"Failed to decode voice sample: {e}")

# Then inside the batch loop, just reuse ref_audio and ref_sr
# Remove the _decode_audio_for_worker call from inside the loop
```

--- 2.4 REMOVE R2 CREDENTIALS FROM REQUEST ---
Lines: ~73-86 (AudiobookRequest dataclass)
Fix: Remove r2_account_id, r2_access_key_id, r2_secret_access_key fields.
Update get_r2_client() to ONLY read from os.environ.
Set up Modal Secrets for R2 credentials instead.

--- 2.5 ADD PROGRESS WEBHOOKS FOR CONCAT/UPLOAD ---
Line: ~461 (progress reporting)
Fix: Add progress webhooks after concatenation (85%) and after upload (95%).

--- 2.6 CORS RESTRICTION ---
Line: ~548-553
Change: `allow_origins=["*"]` → `allow_origins=["https://echomancer-v2.vercel.app"]`

--- 2.7 R2 TIMEOUTS ---
Line: ~106-120 (get_r2_client)
Fix: Add boto3 timeout config:
```python
from botocore.config import Config
config = Config(connect_timeout=30, read_timeout=60)
return boto3.client("s3", ..., config=config)
```

--- 2.8 TRACK FAILED SECTIONS ---
Line: ~439-456 (section generation loop)
Fix: Maintain a failed_sections list and include it in the final webhook.

--- 2.9 ADD WEBHOOK SECRET HEADER ---
Line: ~216-231 (send_webhook_sync)
Fix: Add secret header:
```python
headers = {"X-Webhook-Secret": os.environ.get("WEBHOOK_SECRET", "")}
response = client.post(url, json=payload, headers=headers)
```

--- 2.10 FIX ref_text AUTO-TRANSCRIBE ---
Line: ~445 (model.infer call)
Fix: Either pass a real reference text or transcribe once before the loop.
For now, try using the first sentence of the voice sample's context if available,
or leave empty but document it adds latency.

--- 2.11 IMPROVE PDF EXTRACTION ---
Line: ~367-371
Fix: Use list append instead of string concatenation:
```python
pages = []
for page in doc:
    pages.append(page.get_text())
raw_text = "".join(pages)
```
Also add check for encrypted PDFs:
```python
if doc.is_encrypted or doc.needs_pass:
    raise ValueError("PDF is encrypted or password-protected")
```

================================================================================
PHASE 3: API ROUTE FIXES
================================================================================

--- 3.1 DEDUPLICATION RETURNS DELETED JOBS ---
File: src/app/api/jobs/route.ts
Line: ~28-35
Fix: Add `AND deleted_at IS NULL` to the dedup query.

--- 3.2 RATE LIMITER MEMORY LEAK ---
File: src/lib/rate-limit.ts
Line: ~13-22
Fix: Delete expired entries:
```typescript
if (!entry || now > entry.resetAt) {
  map.delete(ip); // ADD THIS
  map.set(ip, { count: 1, resetAt: now + windowMs });
  return true;
}
```

--- 3.3 R2 LISTFILES PAGINATION ---
File: src/lib/r2-storage.ts
Line: ~139-150
Fix: Implement pagination loop:
```typescript
export async function listFiles(prefix?: string): Promise<string[]> {
  const client = getR2Client();
  const keys: string[] = [];
  let continuationToken: string | undefined;
  do {
    const response = await client.send(
      new ListObjectsV2Command({
        Bucket: R2_BUCKET_NAME,
        Prefix: prefix,
        ContinuationToken: continuationToken,
      })
    );
    keys.push(...(response.Contents || []).map((obj) => obj.Key!).filter(Boolean));
    continuationToken = response.IsTruncated ? response.NextContinuationToken : undefined;
  } while (continuationToken);
  return keys;
}
```

--- 3.4 R2 GETFILE MISSING BODY CHECK ---
File: src/lib/r2-storage.ts
Line: ~102-120
Fix: Add body check:
```typescript
if (!response.Body) {
  throw new Error("Empty response body from R2");
}
```

--- 3.5 YOUTUBE SEARCH TIMEOUTS ---
File: src/app/api/youtube/search/route.ts
Status: DELETE THIS FILE (Phase 0)

--- 3.6 ABORTSIGNAL.TIMEOUT COMPATIBILITY ---
Files: src/app/api/voice/preview/route.ts, src/app/api/health/route.ts
Line: ~94 and ~26
Fix: Replace AbortSignal.timeout with AbortController:
```typescript
const controller = new AbortController();
const timeoutId = setTimeout(() => controller.abort(), 300_000);
try {
  const response = await fetch(url, { signal: controller.signal });
} finally {
  clearTimeout(timeoutId);
}
```

--- 3.7 AUDIO UPLOAD MAGIC BYTES ---
File: src/app/api/audio/upload/route.ts
Line: ~67-76
Fix: Add header signatures for missing formats:
```typescript
const isAac = header[0] === 0xFF && (header[1] === 0xF1 || header[1] === 0xF9);
const isAiff = header[0] === 0x46 && header[1] === 0x4F && header[2] === 0x52 && header[3] === 0x4D;
const isAmr = header[0] === 0x23 && header[1] === 0x21 && header[2] === 0x41;
// Update the validation check to include these
```

--- 3.8 WEBHOOK UPDATES NONEXISTENT JOBS ---
File: src/app/api/jobs/[id]/webhook/route.ts
Line: ~52-54
Fix: Check if job exists before updating:
```typescript
const job = await queryOne<{ id: string }>("SELECT id FROM jobs WHERE id = ?", [id]);
if (!job) {
  return NextResponse.json({ error: "Job not found" }, { status: 404 });
}
```

--- 3.9 HEALTH CHECK MISCLASSIFIES ERRORS ---
File: src/app/api/health/route.ts
Line: ~24-31
Fix: Distinguish error types:
```typescript
checks.modal = response.ok ? "warm" : "error";
```

--- 3.10 DELETE REMOVES SHARED VOICE FILES ---
File: src/app/api/jobs/[id]/route.ts
Line: ~55-77
Fix: Only delete audiobook files, never voice files:
```typescript
const pathsToDelete = [
  job.pdf_storage_path,
  // job.voice_storage_path, // REMOVED — don't delete shared voice files
  job.audio_storage_path,
].filter((p): p is string => Boolean(p));
```

--- 3.11 VOICES DELETE NO CLEANUP ---
File: src/app/api/voices/route.ts
Line: ~82-96
Fix: Delete the audio file before deleting the DB row:
```typescript
const voice = await queryOne<{ storage_path: string }>("SELECT storage_path FROM voices WHERE id = ?", [id]);
if (voice?.storage_path) {
  await deleteFile(voice.storage_path).catch(() => {});
}
await execute("DELETE FROM voices WHERE id = ?", [id]);
```

================================================================================
PHASE 4: FRONTEND FIXES
================================================================================

--- 4.1 NaN POISONING IN VOICE CLIP ---
File: src/app/dashboard/voice/clip/page.tsx
Line: ~31-32
Fix:
```typescript
const rawStart = Number(searchParams.get("startTime"));
const rawEnd = Number(searchParams.get("endTime"));
const start = Number.isFinite(rawStart) && rawStart >= 0 ? rawStart : 0;
const end = Number.isFinite(rawEnd) && rawEnd >= 0 ? rawEnd : 30;
```

--- 4.2 AUDIO LISTENER MEMORY LEAK ---
File: src/app/dashboard/voice/clip/page.tsx
Line: ~111-133 (handlePreview)
Fix: Remove listener on pause:
```typescript
const handlePreview = useCallback(() => {
  const audio = audioRef.current;
  if (!audio || !audioUrl) {
    toast.error("No audio loaded");
    return;
  }
  if (isPlaying) {
    audio.pause();
    setIsPlaying(false);
    // Remove any existing checkTime listeners
    // (track them with a ref or use a named function)
    return;
  }
  audio.currentTime = startTime;
  audio.play().catch(() => {});
  setIsPlaying(true);
  
  const checkTime = () => {
    if (audio.currentTime >= endTime) {
      audio.pause();
      setIsPlaying(false);
      audio.removeEventListener("timeupdate", checkTime);
    }
  };
  audio.addEventListener("timeupdate", checkTime);
  // Store ref to remove on next call
}, [audioUrl, isPlaying, startTime, endTime]);
```

--- 4.3 MOBILE BUTTONS INVISIBLE ---
File: src/app/dashboard/queue/page.tsx
Line: ~257-279
Fix: Change opacity classes:
```
// Change from:
className="... opacity-0 group-hover:opacity-100 ..."
// To:
className="... opacity-100 md:opacity-0 md:group-hover:opacity-100 ..."
```

--- 4.4 PLAYER SPINNER FOREVER ---
File: src/app/dashboard/player/[id]/page.tsx
Line: ~64-80, 226-232
Fix: Add error state:
```typescript
const [error, setError] = useState<string | null>(null);
// In fetchJob catch: setError("Failed to load audiobook");
// In render: if (error) return <ErrorDisplay message={error} />;
```

--- 4.5 POLLING INTERVAL RESET ---
Files: src/app/dashboard/queue/page.tsx, src/app/dashboard/player/[id]/page.tsx
Fix: Use ref-based interval:
```typescript
const fetchRef = useRef(fetchJobs);
fetchRef.current = fetchJobs;
useEffect(() => {
  const hasActive = jobs.some(j => j.status === "processing" || j.status === "queued");
  if (!hasActive) return;
  const id = setInterval(() => fetchRef.current(), 3000);
  return () => clearInterval(id);
}, [jobs.some(j => j.status === "processing" || j.status === "queued")]); // Only depends on boolean
```

--- 4.6 UNHANDLED AUDIO.PLAY() PROMISES ---
Files: src/app/dashboard/voice/clip/page.tsx, src/app/dashboard/player/[id]/page.tsx
Fix: Add .catch(() => {}) to all audio.play() calls.

--- 4.7 DRAG-LEAVE FLICKER ---
File: src/app/page.tsx
Line: ~96-119
Fix:
```typescript
const handleDragLeave = (e: React.DragEvent) => {
  if (!e.currentTarget.contains(e.relatedTarget as Node)) {
    setIsDraggingBook(false);
  }
};
```

--- 4.8 UNCONTROLLED TIME INPUTS ---
File: src/app/dashboard/voice/clip/page.tsx
Line: ~367-411
Fix: Add value prop:
```typescript
<input value={formatTime(startTime)} onChange={...} />
<input value={formatTime(endTime)} onChange={...} />
```

--- 4.9 SYNC TO URL SPAM ---
File: src/app/dashboard/voice/clip/page.tsx
Line: ~48-53
Fix: Debounce syncToUrl:
```typescript
const syncToUrl = useCallback(
  debounce((start: number, end: number) => {
    const params = new URLSearchParams(searchParams.toString());
    params.set("startTime", String(start));
    params.set("endTime", String(end));
    router.replace(`/dashboard/voice/clip?${params.toString()}`, { scroll: false });
  }, 300),
  [searchParams, router]
);
```

--- 4.10 DELETE WITHOUT CONFIRMATION ---
File: src/app/dashboard/queue/page.tsx
Line: ~94-107
Fix: Add confirm():
```typescript
if (!confirm("Delete this audiobook? This cannot be undone.")) return;
```

================================================================================
PHASE 5: MISC CLEANUP
================================================================================

- Remove unused imports after YouTube removal
- Remove `videoId` from Job interface/type definitions
- Update src/app/api/jobs/route.ts to not reference YouTube-related fields
- Make sure the build passes: npm run build
- Make sure no TypeScript errors
- Test the core flow: Upload PDF → Upload voice → Clip voice → Create job

================================================================================
IMPORTANT NOTES
================================================================================

1. The app is currently deployed to Vercel. Push to main branch triggers 
   auto-deploy.
2. Modal is deployed separately via `modal deploy f5_tts_server.py` from the 
   modal/ directory. You need Modal CLI access.
3. The database is Turso (SQLite in the cloud). Schema was already migrated 
   to match code expectations.
4. R2 (Cloudflare) is used for production storage. Local filesystem fallback 
   exists via STORAGE_PATH env var.
5. There is NO authentication currently. The "anonymous" user_id is hardcoded 
   everywhere. That's expected for now — don't try to add auth.
6. The YouTube feature should be COMPLETELY gone — no tabs, no API routes, 
   no references in code, no env vars. Users should only be able to upload 
   their own voice audio files.
7. After all fixes, run `npm run build` to verify no TypeScript errors.
8. Commit with a clear message: "fix: resolve all critical/high bugs and 
   remove YouTube feature"
9. Push to origin/main.

================================================================================
